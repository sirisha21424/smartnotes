spring.application.name=smartnotes-backend

# If DB_URL doesn't exist in environment, fall back to local MySQL
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/smartnotes}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=Ks1981105$

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=${PORT:8080}

# Ollama Configuration
spring.ai.ollama.base-url=http://localhost:11434
spring.ai.ollama.chat.model=llama3.1